// Generated by CoffeeScript 1.8.0
var ball, draw, init, rect;

init = function() {
  var canvas, context, gameField, simpleBall;
  document.getElementById("btn").onclick = function() {
    return alert("click!");
  };
  canvas = document.getElementById("ballpool");
  canvas.width = 800;
  canvas.height = 600;
  context = canvas.getContext("2d");
  simpleBall = ball(context, "#000", 400, 400, 10);
  gameField = rect(context, "#555", 0, 0, 800, 600);
  return draw();
};

draw = function() {
  simpleBall.draw();
  return gameField.draw();
};

rect = function(context, color, x, y, width, height) {
  this.context = context;
  this.color = color;
  this.x = x;
  this.y = y;
  this.width = width;
  this.height = height;
  this.draw = function() {
    this.context.fillStyle = this.color;
    return this.context.fillRect(this.x, this.y, this.width, this.height);
  };
  return this;
};

ball = function(context, color, x, y, radius) {
  this.context = context;
  this.color = color;
  this.centerX = x;
  this.centerY = y;
  this.radius = radius;
  this.draw = function() {
    this.context.beginPath();
    this.context.arc(centerX, centerY, radius, 0, Math.PI * 2, false);
    this.context.stroke();
    this.context.fillStyle = 'red';
    return this.context.fill();
  };
  return this;
};
